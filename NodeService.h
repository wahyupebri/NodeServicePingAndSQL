// NodeService.h : Include file for standard system include files,
// or project specific include files.

#pragma once
#include <stdio.h>
#include <iostream>
#include "pingext.h"
#include <windows.h>

int serverWeb() {
    //struct mg_server* server = mg_create_server(NULL, NULL);

    //mg_set_request_handler(server, "/api", request_handler, NULL);

    //printf("Starting Civetweb on http://localhost:8080\n");
    //for (;;) {
    //    mg_poll_server(server, 1000); // 1000 milliseconds
    //}

    //mg_destroy_server(&server);
    return 0;
}
// TODO: Reference additional headers your program requires here.
